#include <stdlib.h>
#include <time.h>
#include <stdio.h>
/**
*main - Digits
*
*Return: Always 0 (Success)
*/
int main(void)
{
char chara;
for (chara = 'z'; chara >= 'a'; chara++)
{
putchar(chara);
}
putchar('\n');
return (0);
}
